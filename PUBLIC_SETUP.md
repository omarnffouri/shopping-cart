# Local service configuration

This public snapshot intentionally omits the backend service-account credential and clears example password values.

Provide service credentials through a local untracked file or a secret manager. Never commit a production service-account JSON file. Replace `REPLACE_WITH_PAYMENT_SECRET_KEY` with a secret supplied through the runtime environment.
